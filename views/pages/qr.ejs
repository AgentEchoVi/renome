<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QR Menu &mdash; Renome</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Inter', sans-serif;
      background: #f4f1ec;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 16px;
    }

    .qr {
      background: #fff;
      border-radius: 20px;
      width: 300px;
      padding: 24px 22px 20px;
      text-align: center;
      box-shadow: 0 8px 32px rgba(0,0,0,0.08);
      position: relative;
    }

    .qr::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, #2d5a27, #4a8c3f, #2d5a27);
      border-radius: 20px 20px 0 0;
    }

    /* --- Header --- */
    .qr__logo {
      width: 36px;
      height: 36px;
      margin: 0 auto 6px;
      display: block;
    }

    .qr__name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 22px;
      font-weight: 700;
      color: #1a1a1a;
      letter-spacing: 3px;
      text-transform: uppercase;
      line-height: 1;
      margin-bottom: 3px;
    }

    .qr__type {
      font-size: 8px;
      color: #aaa;
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 14px;
    }

    .qr__type span { color: #ccc; margin: 0 3px; }

    /* --- QR --- */
    .qr__code {
      border: 2px solid #2d5a27;
      border-radius: 14px;
      padding: 10px;
      display: inline-block;
      margin-bottom: 14px;
    }

    .qr__code img {
      display: block;
      width: 150px;
      height: 150px;
    }

    /* --- Steps --- */
    .qr__steps {
      display: flex;
      justify-content: center;
      gap: 6px;
      margin-bottom: 12px;
    }

    .qr__step {
      flex: 1;
      max-width: 80px;
      padding: 8px 4px;
      background: #f8f7f4;
      border-radius: 10px;
      font-size: 9px;
      color: #666;
      line-height: 1.3;
    }

    .qr__step-icon {
      display: block;
      margin: 0 auto 4px;
      line-height: 1;
    }

    .qr__step-icon svg {
      width: 22px;
      height: 22px;
      stroke: #2d5a27;
      fill: none;
      stroke-width: 1.8;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .qr__step-ro { display: block; }
    .qr__step-ru { display: block; color: #aaa; font-size: 8px; }

    /* --- URL --- */
    .qr__url {
      display: inline-block;
      padding: 5px 14px;
      background: #f5f3ef;
      border-radius: 20px;
      font-size: 10px;
      color: #2d5a27;
      font-weight: 600;
      text-decoration: none;
      letter-spacing: 0.2px;
      margin-bottom: 10px;
      transition: background 0.2s;
    }

    .qr__url:hover { background: #ece9e2; }

    /* --- Divider --- */
    .qr__sep {
      width: 24px;
      height: 1px;
      background: #e0e0e0;
      margin: 0 auto 8px;
    }

    /* --- Footer --- */
    .qr__info {
      font-size: 9px;
      color: #bbb;
      line-height: 1.6;
    }

    /* --- Print btn --- */
    .qr__print {
      margin-top: 16px;
    }

    .qr__print-btn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 8px 24px;
      background: #2d5a27;
      color: #fff;
      border: none;
      border-radius: 50px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s;
    }

    .qr__print-btn:hover { background: #1e3d1a; }
    .qr__print-btn svg { width: 13px; height: 13px; }

    /* ====== PRINT ====== */
    @media print {
      @page {
        size: 80mm 120mm;
        margin: 0;
      }

      html, body {
        width: 80mm;
        height: 120mm;
        background: #fff;
        padding: 0;
        margin: 0;
        min-height: 0;
        overflow: hidden;
      }

      .qr {
        box-shadow: none;
        border-radius: 0;
        width: 80mm;
        height: 120mm;
        padding: 5mm 4mm 4mm;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        page-break-inside: avoid;
        break-inside: avoid;
      }

      .qr::before { display: none; }
      .qr__print { display: none !important; }

      .qr__step,
      .qr__url {
        background: #f0f0f0;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
      }
      .qr__code {
        border-color: #2d5a27;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
      }

      .qr__logo { width: 24px; height: 24px; margin-bottom: 2px; }
      .qr__name { font-size: 16px; margin-bottom: 1px; }
      .qr__type { font-size: 6px; margin-bottom: 6px; }
      .qr__code { padding: 5px; margin-bottom: 5px; border-radius: 6px; }
      .qr__code img { width: 28mm; height: 28mm; }
      .qr__steps { gap: 3px; margin-bottom: 5px; }
      .qr__step { padding: 4px 2px; border-radius: 5px; font-size: 7px; }
      .qr__step-icon svg { width: 14px; height: 14px; }
      .qr__step-icon { margin-bottom: 2px; }
      .qr__step-ru { font-size: 6px; }
      .qr__url { font-size: 7px; padding: 2px 8px; margin-bottom: 4px; }
      .qr__sep { margin-bottom: 3px; }
      .qr__info { font-size: 6px; }
    }

    /* ====== Mobile ====== */
    @media (max-width: 340px) {
      .qr { width: 100%; padding: 20px 16px 16px; }
      .qr__name { font-size: 20px; }
      .qr__code img { width: 130px; height: 130px; }
      .qr__step { max-width: 70px; }
    }
  </style>
</head>
<body>
  <div class="qr">
    <img src="/img/logo-clean.png" alt="Renome" class="qr__logo">
    <div class="qr__name">Renome</div>
    <div class="qr__type">Italian Caf&eacute; <span>&middot;</span> Итальянское Кафе</div>

    <div class="qr__code">
      <img src="https://api.qrserver.com/v1/create-qr-code/?size=440x440&data=https://renome.zeabur.app&color=2d5a27&bgcolor=ffffff" alt="QR">
    </div>

    <div class="qr__steps">
      <div class="qr__step">
        <span class="qr__step-icon"><svg viewBox="0 0 24 24"><path d="M15 3h6v6M9 21H3v-6"/><path d="M21 3l-7 7M3 21l7-7"/><rect x="7" y="7" width="3" height="3" rx=".5"/><rect x="14" y="14" width="3" height="3" rx=".5"/></svg></span>
        <span class="qr__step-ro">Scaneaz&#259;</span>
        <span class="qr__step-ru">Сканируй</span>
      </div>
      <div class="qr__step">
        <span class="qr__step-icon"><svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="8" y1="13" x2="16" y2="13"/><line x1="8" y1="17" x2="14" y2="17"/></svg></span>
        <span class="qr__step-ro">Alege</span>
        <span class="qr__step-ru">Выбери</span>
      </div>
      <div class="qr__step">
        <span class="qr__step-icon"><svg viewBox="0 0 24 24"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M17 11l2 2 4-4"/></svg></span>
        <span class="qr__step-ro">Comand&#259;</span>
        <span class="qr__step-ru">Закажи</span>
      </div>
    </div>

    <a href="https://renome.zeabur.app" class="qr__url">renome.zeabur.app</a>

    <div class="qr__sep"></div>

    <div class="qr__info">
      str. Calea Ie&#537;ilor 57, B&#259;l&#539;i &middot; ул. Каля Ешилор 57<br>
      10:00 &mdash; 01:00
    </div>

    <div class="qr__print">
      <button class="qr__print-btn" onclick="window.print()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>
        Print
      </button>
    </div>
  </div>
</body>
</html>
