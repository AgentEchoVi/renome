<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QR Menu &mdash; Renome</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Inter', sans-serif;
      background: #f4f1ec;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 16px;
    }

    .qr {
      background: #fff;
      border-radius: 20px;
      width: 300px;
      padding: 24px 22px 20px;
      text-align: center;
      box-shadow: 0 8px 32px rgba(0,0,0,0.08);
      position: relative;
    }

    .qr::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, #2d5a27, #4a8c3f, #2d5a27);
      border-radius: 20px 20px 0 0;
    }

    /* --- Header --- */
    .qr__logo {
      width: 36px;
      height: 36px;
      margin: 0 auto 6px;
      display: block;
    }

    .qr__name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 22px;
      font-weight: 700;
      color: #1a1a1a;
      letter-spacing: 3px;
      text-transform: uppercase;
      line-height: 1;
      margin-bottom: 3px;
    }

    .qr__type {
      font-size: 8px;
      color: #aaa;
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 14px;
    }

    .qr__type span { color: #ccc; margin: 0 3px; }

    /* --- QR --- */
    .qr__code {
      border: 2px solid #2d5a27;
      border-radius: 14px;
      padding: 10px;
      display: inline-block;
      margin-bottom: 12px;
    }

    .qr__code img {
      display: block;
      width: 160px;
      height: 160px;
    }

    /* --- CTA --- */
    .qr__cta {
      font-family: 'Cormorant Garamond', serif;
      font-weight: 600;
      color: #1a1a1a;
      line-height: 1.2;
      margin-bottom: 4px;
    }

    .qr__cta-ro { font-size: 17px; }
    .qr__cta-ru { font-size: 14px; color: #888; font-weight: 500; }

    /* --- Order hint --- */
    .qr__hint {
      font-size: 10px;
      color: #aaa;
      line-height: 1.5;
      margin-bottom: 10px;
    }

    .qr__hint span {
      display: inline-block;
      margin: 0 2px;
      color: #ccc;
    }

    /* --- URL --- */
    .qr__url {
      display: inline-block;
      padding: 5px 14px;
      background: #f5f3ef;
      border-radius: 20px;
      font-size: 10px;
      color: #2d5a27;
      font-weight: 600;
      text-decoration: none;
      letter-spacing: 0.2px;
      margin-bottom: 10px;
      transition: background 0.2s;
    }

    .qr__url:hover { background: #ece9e2; }

    /* --- Divider --- */
    .qr__sep {
      width: 24px;
      height: 1px;
      background: #e0e0e0;
      margin: 0 auto 8px;
    }

    /* --- Footer --- */
    .qr__info {
      font-size: 9px;
      color: #bbb;
      line-height: 1.6;
    }

    /* --- Print btn --- */
    .qr__print {
      margin-top: 16px;
    }

    .qr__print-btn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 8px 24px;
      background: #2d5a27;
      color: #fff;
      border: none;
      border-radius: 50px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s;
    }

    .qr__print-btn:hover { background: #1e3d1a; }
    .qr__print-btn svg { width: 13px; height: 13px; }

    /* ====== PRINT — одна страница ====== */
    @media print {
      @page {
        size: 80mm 110mm;
        margin: 0;
      }

      html, body {
        width: 80mm;
        height: 110mm;
        background: #fff;
        padding: 0;
        margin: 0;
        min-height: 0;
        overflow: hidden;
      }

      .qr {
        box-shadow: none;
        border-radius: 0;
        width: 80mm;
        height: 110mm;
        padding: 8mm 6mm 5mm;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        page-break-inside: avoid;
        break-inside: avoid;
      }

      .qr::before { display: none; }
      .qr__print { display: none !important; }
      .qr__url { background: #eee; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .qr__code { border-color: #2d5a27; -webkit-print-color-adjust: exact; print-color-adjust: exact; }

      .qr__logo { width: 28px; height: 28px; margin-bottom: 3px; }
      .qr__name { font-size: 18px; margin-bottom: 2px; }
      .qr__type { font-size: 7px; margin-bottom: 8px; }
      .qr__code { padding: 6px; margin-bottom: 6px; border-radius: 8px; }
      .qr__code img { width: 32mm; height: 32mm; }
      .qr__cta { margin-bottom: 2px; }
      .qr__cta-ro { font-size: 13px; }
      .qr__cta-ru { font-size: 11px; }
      .qr__hint { font-size: 8px; margin-bottom: 4px; }
      .qr__url { font-size: 8px; padding: 3px 10px; margin-bottom: 5px; }
      .qr__sep { margin-bottom: 4px; }
      .qr__info { font-size: 7px; }
    }

    /* ====== Mobile ====== */
    @media (max-width: 340px) {
      .qr { width: 100%; padding: 20px 18px 16px; }
      .qr__name { font-size: 20px; }
      .qr__code img { width: 140px; height: 140px; }
    }
  </style>
</head>
<body>
  <div class="qr">
    <img src="/img/logo-clean.png" alt="Renome" class="qr__logo">
    <div class="qr__name">Renome</div>
    <div class="qr__type">Italian Caf&eacute; <span>&middot;</span> Итальянское Кафе</div>

    <div class="qr__code">
      <img src="https://api.qrserver.com/v1/create-qr-code/?size=440x440&data=https://renome.zeabur.app&color=2d5a27&bgcolor=ffffff" alt="QR">
    </div>

    <div class="qr__cta">
      <div class="qr__cta-ro">Meniu &amp; Comand&#259; online</div>
      <div class="qr__cta-ru">Меню и Заказ онлайн</div>
    </div>

    <div class="qr__hint">
      Scaneaz&#259; QR <span>|</span> Сканируй QR
    </div>

    <a href="https://renome.zeabur.app" class="qr__url">renome.zeabur.app</a>

    <div class="qr__sep"></div>

    <div class="qr__info">
      str. Calea Ie&#537;ilor 57, B&#259;l&#539;i <span>&middot;</span> ул. Каля Ешилор 57<br>
      10:00 &mdash; 01:00
    </div>

    <div class="qr__print">
      <button class="qr__print-btn" onclick="window.print()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>
        Print
      </button>
    </div>
  </div>
</body>
</html>
